# Release Notes

## What's New

- Added CMake presets for standardized build configurations
- Migrated CI from GitLab to GitHub Actions
- Added GitHub release workflow

## Bug Fixes

- Fixed clang-format workflow to actually fail on formatting issues
- Fixed macOS dependency installation for newer macOS versions
